{
  "name": "backend",
  "version": "0.1.0",
  "bin": {
    "backend": "bin/backend.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "deploy": "cdk deploy --all --outputs-file outputs.json",
    "test": "jest"
  },
  "devDependencies": {
    "@aws-solutions-constructs/aws-eventbridge-lambda": "^2.45.0",
    "aws-cdk": "^2.102.0",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.438.0",
    "@aws-sdk/lib-dynamodb": "^3.445.0",
    "@aws-sdk/util-dynamodb": "^3.438.0",
    "@cloudcomponents/cdk-responsive-email-template": "^2.1.0",
    "@sparticuz/chromium": "^112.0.2",
    "aws-cdk-lib": "^2.102.0",
    "constructs": "^10.0.0",
    "puppeteer-core": "19.8"
  }
}
