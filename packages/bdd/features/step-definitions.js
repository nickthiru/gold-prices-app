const { Given, When, Then } = require("@cucumber/cucumber");
const { expect } = require("expect");

Given('I login', function (distance) {
  // To do
});

When('I visit the homepage', function (message) {
  // To do
});

Then('I should see the latest prices displayed', function () {
  // To do
});