// This method will take the email template and fill in the data where needed.

module.exports = function populateEmailTemplate(data, template) {
  console.log("(+) Inside 'populateEmailTemplate()'");
  console.log("(+) data: " + data);
  console.log("(+) template: " + template);

  // Make a copy of the template

  // Fill in the data. How?

  // Return populatedTemplate
}